import { GetFiltersWithStatusPipe } from './get-filters-with-status.pipe';

describe('GetFiltersWithStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new GetFiltersWithStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
