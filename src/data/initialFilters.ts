import { allCheckboxFilters } from './allCheckboxFilters';
import { suggestedClients } from './suggestedClients';
export const initialFilters = {
  'statuses': [ allCheckboxFilters[ 0 ], allCheckboxFilters[ 3 ] ],
  'clients': [ suggestedClients[ 0 ], suggestedClients[ 10 ], suggestedClients[ 2 ] ]
};
